[DEFAULT]
# Yoyo migration configuration for Budget Buddy

# Database connection from environment variable
database = ${DATABASE_URL}

# Directory containing migration files
sources = db/migrations

# Batch mode (non-interactive)
batch_mode = off

# Verbosity level
verbosity = 1
